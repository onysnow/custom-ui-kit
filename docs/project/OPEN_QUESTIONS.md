# Open Questions

1. What failures does EXP-001 expose after the canonical workspace is installed?
2. Can EXP-006 prove environment-dependent lowering without mutating Material IR?
3. Which browser paint-boundary cases can safely participate in optional GPU rendering?
